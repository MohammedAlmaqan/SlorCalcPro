import { ScreenScaffold } from '@/components/ScreenScaffold';

export default function CatalogScreen() {
  return (
    <ScreenScaffold
      title="Catalog"
      module="Component Database"
      description="Searchable, editable offline database of PV panels, inverters, batteries and cables. Favorites supported."
      phase="Phase 2 (Data layer) + Phase 3 (Wizard UI)"
    />
  );
}
