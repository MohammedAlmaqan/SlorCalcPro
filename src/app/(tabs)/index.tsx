import { ScreenScaffold } from '@/components/ScreenScaffold';

export default function ProjectsScreen() {
  return (
    <ScreenScaffold
      title="Projects"
      module="Project Management"
      description="Create, save, duplicate and manage solar system design projects. JSON backup & restore included."
      phase="Phase 3 (Wizard UI)"
    />
  );
}
