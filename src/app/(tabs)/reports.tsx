import { ScreenScaffold } from '@/components/ScreenScaffold';

export default function ReportsScreen() {
  return (
    <ScreenScaffold
      title="Reports"
      module="Reports & Export"
      description="Offline PDF design summaries, BOM CSV export and single-line diagram generation."
      phase="Phase 4 (Viz & Reports)"
    />
  );
}
